# 100W 数控全桥 LLC 开发日记

> 项目：CSS024D-DCDC-V2.0 复刻版  
> 控制器：TI TMS320F28034  
> 拓扑：全桥 LLC  
> 输入：30V DC（设计考虑 24~36V）  
> 输出目标：12V / 8.3A / 100W  
> 当前主线：从零自主开发 F28034 数字控制软件，不复制 CSS024D 原代码  
> 最后更新：2026-08-17  
> 当前阶段：Stage 6 物理 plant 模型/PI 候选纯离线整定（MODEL_HARDWARE_CONSISTENCY_PASS_V1_2 + STAGE6_PI_SIL_TUNING_V2_1_PASS）；固件门锁死，待授权

---

## 1. 项目开发原则

本项目不以“直接改现成源码跑起来”为目标，而是以真正理解和掌握 F28034 数字 LLC 控制为目标。

开发顺序：

```text
需求
→ 架构
→ 状态机
→ 时间系统
→ PWM
→ PFM
→ ADC
→ 保护
→ 开环功率 Bring-up
→ 软启动
→ VOUT 控制
→ PI / PFM 闭环
→ 带载
→ 保护验证
→ 效率与温升优化
```

安全原则：

```text
先安全稳定跑起来
→ 再逐步增加能量
→ 再逐步逼近12V
→ 再做连续运行
→ 最后带载到100W
```

当前明确禁止：

```text
- 不直接连续150kHz运行
- 不直接追12V
- 不进入PI闭环
- 不扫频
- 不带100W
- 不取消Comparator/TZ
- 不把当前6-sample qualification当最终OCP参数
- 不因为ADC正常就认为Vds/ZVS正常
```

---

# 2. 当前硬件 / 软件基准

## 2.1 功率级

```text
Vin      = 30V
Vout     = 12V目标
Pout     = 100W
Iout     ≈ 8.3A

拓扑     = Full-Bridge LLC
驱动     = SI8233BD
控制器   = TMS320F28034

变压器   = PQ3230
原边     = 5T
副边     = 4T，中间抽头
```

当前磁件实测 / 设计数量级：

```text
Lr ≈ 3.15~3.4 uH
Lm ≈ 17 uH
Cr ≈ 3 uF
fr ≈ 50 kHz
```

重新计算后的模型表明：

```text
50kHz 是谐振点
并不是30V版本的额定工作频率

30V → 12V / 100W
理论额定工作区约在150~155kHz附近
```

---

## 2.2 控制器基础

```text
SYSCLK ≈ 60MHz
TBCLK  ≈ 60MHz
PLLCR  = 6
```

PFM 严格公式：

```text
TBPRD = round(60MHz / fs) - 1
CMPA  = (TBPRD + 1) / 2
```

150kHz：

```text
TBPRD = 399
CMPA  = 200
```

PWM采用：

```text
TB_COUNT_UP
互补输出
DBRED = 36
DBFED = 36
```

死区理论约：

```text
36 / 60MHz ≈ 600ns
```

---

# 3. Bring-up 时间线

---

## 3.1 Stage 0：SAFE 上电状态

### 目标

确认 DSP、PLL、JTAG、GPIO、ADC、Timer 和 PWM 安全默认状态。

### 结果

```text
XDS100v2 + CCS 正常连接
PLLCR = 6
SYSCLK ≈ 60MHz
PLL locked
PWM默认关闭
OST锁存
ADC正常
Timer0正常
绿色LED正常
```

### 结论

```text
Stage 0 PASS
```

最重要的安全基线：

> MCU 上电后不允许自动产生功率 PWM。

---

## 3.2 Stage 1：固定 150kHz PWM

CNT3/CNT4 断开，只验证驱动，不让 LLC 谐振回路传递能量。

### 实测

```text
PWM频率 ≈ 150kHz
Vgs ≈ 12~13V
死区 ≈ 0.5~0.6us
未发现明显上下管重叠
```

### 结论

```text
Stage 1 PASS
```

---

## 3.3 Stage 2：PFM 手动调频

验证频率控制是否真正成立。

实测点：

```text
150kHz
100kHz
70kHz
```

CNT3/CNT4 断开时，30V输入实际电流约：

```text
150kHz ≈ 100mA
100kHz ≈ 81mA
70kHz  ≈ 69mA
```

符合开关频率下降后驱动 / 开关损耗降低的趋势。

### 结论

```text
Stage 2 PASS
```

---

# 4. ADC 调试

## 4.1 第一个软件语义 Bug：IIR filtered_raw 错误

最初程序把 IIR 滤波内部的 16× 累加器直接暴露成：

```text
filtered_raw
```

导致调试变量看起来比真实 ADC 值大约16倍。

修复：

```text
filter_acc
↓ >>4
filtered_raw
```

例如：

```text
Vout raw      ≈ 15
filtered_raw  ≈ 13
filter_acc    ≈ 209
```

### 结论

ADC采样链和滤波链：

```text
PASS
```

### 经验

> 调试变量的“名字”也属于接口设计。  
> 一个数虽然算对了，但语义命名错误，也会严重误导调试。

---

## 4.2 IPRI / IOUT 比例确认

必须明确：

```text
0.33V/A 是 IOUT 采样比例
不是 IPRI
```

IPRI 真正的 A/V 比例目前仍未完成标定。

---

# 5. Comparator / Trip Zone 保护链

## 5.1 软件强制 Trip 验证

执行：

```text
g_force_trip_request = 1
```

结果：

```text
PWM OFF
FAULT进入
OST锁存
trip_count增加
不会自动恢复
```

显式 Reset 后才允许恢复。

### 结论

```text
Stage 4A PASS
```

---

## 5.2 实板 Comparator 路径确认

实际保护路径：

```text
T2 CT
→ MB10S D10
→ R32 = 10Ω
→ IPRI
→ ADCINA2 / COMP1A
```

数字路径：

```text
COMP1OUT GPIO42
→ PCB
→ GPIO15 / TZ1
```

做过 BOARD_LOOPBACK_DIAGNOSTIC：

```text
GPIO42输出：
0 → 1 → 0

GPIO15输入：
正确读到：
0 → 1 → 0
```

### 结论

```text
Comparator → TZ1 PCB路径 PASS
```

---

# 6. 真实硬件错误：D10 整流桥极性反接

这是本项目第一次明确定位到的硬件设计 / 实装错误。

错误连接：

```text
D10 + → SGND
D10 - → IPRI
```

正确应为：

```text
D10 + → IPRI
D10 - → SGND
```

已实物返修。

返修后：

```text
D10+ → D10- 路径约10Ω
与R32=10Ω一致

真实发波时 IPRI 出现正向响应
```

### 经验

> 原理图、计算书、教程和实际 PCB 都不能盲信。  
> 最终必须用实物通断、电压和波形建立证据链。

---

# 7. 首次真实功率测试与 IPRI 首沿尖峰

CNT3/CNT4 接通后，开始第一次真实功率 Probe。

最初 2ms Probe：

```text
被 Comparator/TZ 提前终止
```

ADC却显示：

```text
Ipri peak 只有几个LSB
```

后来示波器发现：

```text
IPRI存在非常快的首沿尖峰
ADC完全捕捉不到
```

DAC=300 的 single-cycle probe 仍然很早 Trip：

```text
stop_tbctr ≈ 17
约0.28us后触发
```

说明事件发生在亚微秒级。

### 关键认识

> 高速硬件过流保护不能依赖软件 ADC peak。  
> ADC“没看见”不等于高速电流尖峰不存在。

---

# 8. TZ1 qualification

为了区分极窄首沿毛刺与真正持续过流，GPIO15/TZ1 临时加入：

```text
GPAQSEL1.GPIO15 = 2
6-sample qualification

QUALPRD1 = 1
```

约百ns级资格窗口。

结果：

```text
首沿窄毛刺不再提前触发TZ
```

### 注意

当前 qualification 只是：

```text
Bring-up诊断参数
```

不是最终 OCP 参数。

最终必须结合：

```text
IPRI真实比例
CT匝比
真实负载
短路测试
示波器波形
```

重新设计。

---

# 9. 第一个严重软件安全 Bug：EPWM1_INT 没真正接到 ISR

加入 qualification 后，Comparator 不再提前帮忙关断。

结果反而暴露了一个严重问题：

```text
Single-cycle Probe本应一个周期后自动OST
实际PWM一直运行
直到DSS暂停CPU才停止
```

立即停止测试，断开 CNT3/CNT4，回到无能量环境定位。

### 根因

PIE Vector Table 中：

```text
EPWM1_INT
```

仍然指向：

```text
rsvd_ISR
```

运行时的 ISR 绑定没有真正生效。

修复：

```text
PieVectTable.EPWM1_INT → EPWM1_INT_ISR
IER = 7
```

无能量验证：

```text
150kHz
运行完整1周期 ≈ 6.67us
→ EPWM1 ISR
→ 自动 OST
```

随后真实功率 single-cycle 也 PASS。

### 极重要经验

> 原来的 Comparator 提前 Trip，实际上掩盖了软件自动停机链已经坏掉。  
> 每一条安全链都必须单独证明，而不能只看“最后PWM确实停了”。

---

# 10. 第二个严重软件安全 Bug：ISR 写 TZFRC 忘记 EALLOW

后来15-cycle诊断时出现：

```text
软件状态：
PWM = 0

但硬件：
TZFLG.OST = 0
```

继续追查。

### 根因

ISR 中执行：

```c
EPwm1Regs.TZFRC.bit.OST = 1;
```

但是没有：

```c
EALLOW;
...
EDIS;
```

F28034 Trip-Zone 相关寄存器受保护，因此写操作被硬件忽略。

### 修复

```c
EALLOW;
EPwm1Regs.TZFRC.bit.OST = 1;
EDIS;
```

修复后：

```text
g_probe_tzflg_immediate = 1
g_probe_tzflg_read2 = 1
g_probe_tzflg_read3 = 1
g_probe_tzflg_after_state_update = 1

TZFLG.all = 0x4
```

### 结论

```text
OST真正硬件锁存 PASS
```

### 经验

> 软件变量 `pwm_enabled=0` 不能证明硬件真的封波。  
> 最终安全证据必须来自硬件状态寄存器和真实输出。

---

# 11. Probe ISR 延迟问题

真实功率初期测得：

```text
CTR=ZERO → EPWM1 ISR
≈165 ticks
≈2.75us

ISR → OST
≈46 ticks
≈0.77us
```

150kHz 周期只有：

```text
6.67us
```

因此2.75us入口延迟太大。

---

## 11.1 PROBE_INTERRUPT_ISOLATION

Probe模式临时屏蔽：

```text
CPU Timer0 / Group1 快任务
ADC软件中断
其它非必要Group1中断
```

保留：

```text
EPWM1_INT
Comparator/TZ硬件保护
```

结果：

```text
无能量：
IRQ latency ≈26 ticks ≈0.43us

真实功率：
IRQ latency ≈25 ticks ≈0.42us
```

ISR→OST：

```text
≈46~65 ticks
≈0.77~1.08us
```

### 结论

原始2.75us主要来自：

```text
Group1中断竞争
```

而不是功率级导致 CPU 变慢。

---

# 12. 真实功率周期阶梯

全部在：

```text
150kHz
```

完成：

```text
1 cycle    ≈ 6.67us    PASS
3 cycles   ≈ 20us      PASS
15 cycles  ≈ 100us     PASS
75 cycles  ≈ 500us     PASS
150 cycles ≈ 1ms       PASS
```

共同结果：

```text
fault = 0
trip  = 0
OST最终 = 1
PWM最终 = 0
```

---

# 13. 示波器暂时不可用后的替代方案：ePWM1 SOCA ADC Capture

普通软件触发 ADC 无法看到1ms以内真实 VOUT 建立过程。

因此改为：

```text
ePWM1 SOCA
→ 每个PWM周期固定相位触发 ADC
→ 每周期记录一次 VOUT
```

不使用 ADC CPU ISR，避免重新引入 Group1 中断竞争。

---

## 13.1 150-cycle / 1ms 首次完整 VOUT 轨迹

结果：

```text
sample_count = 150

first = 8
last  = 966
min   = 1
max   = 966
```

每10周期：

```text
cycle 1   = 8
cycle 10  = 14
cycle 20  = 169
cycle 30  = 234
cycle 40  = 299
cycle 50  = 361
cycle 60  = 427
cycle 70  = 487
cycle 80  = 549
cycle 90  = 605
cycle 100 = 674
cycle 110 = 736
cycle 120 = 798
cycle 130 = 860
cycle 140 = 917
cycle 150 = 966
```

### 里程碑

这是第一次明确证明真实能量已经走完整条链：

```text
30V输入
→ 全桥
→ LLC谐振腔
→ 变压器
→ 副边整流
→ 输出电容
```

---

# 14. ADC raw / LSB 的含义

F28034 ADC：

```text
12-bit
0~4095 counts
约0~3.3V
```

理论上：

```text
1 ADC count ≈ 3.3 / 4096
≈ 0.806mV（ADC引脚）
```

当前 VOUT 采样理论比例约：

```text
Vadc ≈ 0.1 × Vout
```

因此理论：

```text
1 raw ≈ 8.06mV 输出侧

1200 raw ≈ 9.7V
1300 raw ≈ 10.5V
1400 raw ≈ 11.3V
1450 raw ≈ 11.7V

理论12V ≈ 1489 raw
```

注意：

> 以上仍是理论换算，不是实板最终标定结果。

---

# 15. VOUT_LIMITED_POWER_PROBE

控制逻辑升级为：

```text
VOUT达到目标
→ 自动OST

或
MAX_CYCLES到达
→ 自动OST

或
Comparator Fault
→ 硬件TZ关断
```

这标志着测试方式从：

```text
“开多久看看”
```

升级成：

```text
“输出到允许位置就自己停”
```

---

## 15.1 1200 raw

```text
completed = 195
stop_reason = VOUT_TARGET_REACHED
stop_raw = 1205

fault=0
trip=0
OST=1
PWM=0
```

PASS。

---

## 15.2 1300 raw

加入：

```text
POST_OST_VOUT_CAPTURE = 100 samples
```

结果：

```text
completed = 214
stop_raw = 1304

post_first = 1274
post_max   = 1274
post_last  = 1253
```

关断后没有继续上冲。

PASS。

---

## 15.3 1400 raw

```text
completed = 234
stop_raw = 1402

post_first = 1367
post_max   = 1367
post_last  = 1345
```

VOUT轨迹：

```text
cycle 20  = 160
cycle 30  = 226
cycle 40  = 292
cycle 50  = 358
cycle 60  = 416
cycle 70  = 483
cycle 80  = 545
cycle 90  = 603
cycle 100 = 664
cycle 110 = 725
cycle 120 = 786
cycle 130 = 842
cycle 140 = 904
cycle 150 = 964
cycle 160 = 1021
cycle 170 = 1071
cycle 180 = 1130
cycle 190 = 1181
cycle 200 = 1234
cycle 210 = 1282
cycle 220 = 1329
cycle 230 = 1373
cycle 234 = 1402
```

表现为：

```text
平稳
单调
逐渐收坡
```

PASS。

---

## 15.4 1450 raw

当前最高一次真实功率受控测试。

```text
completed = 248
stop_reason = VOUT_TARGET_REACHED

stop_raw = 1455
max_raw = 1455

fault=0
trip=0
OST=1
PWM=0
```

POST OST：

```text
post_first = 1425
post_max   = 1425
post_last  = 1403
```

最后阶段：

```text
cycle 200 = 1226
cycle 210 = 1277
cycle 220 = 1326
cycle 230 = 1367
cycle 240 = 1417
cycle 248 = 1455 STOP
```

### 结论

```text
1450 raw PASS
```

按计划：

```text
停止继续抬电压
不直接追1489 raw
```

---

# 16. 150kHz 当前稳定性评价

目前可以认定：

```text
150kHz = 可信 Bring-up 基准频率
```

已经实板证明：

```text
PWM稳定
PFM正确
驱动正常
死区正常
同步ADC正常
Comparator/TZ正常
OST正常
真实LLC传能正常
VOUT受控建立正常
1450 raw附近仍可主动停机
```

但仍不能认定：

```text
150kHz长期稳态已验证
150kHz满载100W已验证
ZVS已验证
Vds尖峰已验证
最终OCP参数已验证
```

---

# 17. 理论模型与实板趋势

目前核心方向已经开始吻合：

```text
fr ≈ 50kHz
但
30V → 12V额定区需要150~155kHz附近
```

实板在150kHz下已经能够：

```text
从0V附近
持续向12V方向充电
```

且 VOUT 上升轨迹稳定。

因此：

```text
“50kHz是谐振点，不是30V版本额定工作频率”
```

这一重新计算后的结论已经获得实物趋势支持。

但还没有完成：

```text
12V / 8.3A / 100W
稳态工作频率实测
```

所以最终模型闭环仍需后续带载完成。

---

# 18. 当前任务：CALIBRATION_HOLD_PROBE

由于示波器暂时不可用，普通万用表也无法直接观察毫秒级 VOUT。

因此下一步目标：

> 用低能量 Burst 将 VOUT 保持在约11V附近几百毫秒到几秒，让万用表可以稳定读数，从而完成 ADC → 实际电压标定。

---

## 18.1 CALIBRATION_HOLD 控制逻辑

初始充电：

```text
150kHz
VOUT充到1400 raw
→ OST
```

进入 HOLD：

```text
VOUT <= 1380
→ 只允许补2个150kHz周期
→ 再OST

继续等待VOUT下降

VOUT再次 <= 1380
→ 再补2周期
```

约束：

```text
VOUT_HIGH_RAW = 1400
VOUT_LOW_RAW  = 1380

HARD_LIMIT = 1450

MAX_PACKET_CYCLES = 2
MAX_TOTAL_EXTRA_CYCLES = 600

第一阶段 HOLD_TIME = 500ms
通过后才考虑2s
```

全程保持：

```text
Comparator/TZ
硬件OST
禁止连续PWM
```

---

# 19. ADC Factory Calibration 已确认

由于当前使用 RAM Debug，专门检查了 Device_cal。

程序：

```text
System_Init()
→ (*Device_cal)()
```

实测：

```text
AdcRegs.ADCREFTRIM.all = 7540
AdcRegs.ADCOFFTRIM.all = 11
AdcRegs.ADCCTL1.all    = 17120
```

说明：

```text
ADC Factory Trim 已明确执行
```

不是假设 Boot ROM 已经做过。

---

# 20. CALIBRATION_HOLD 第一次500ms测试异常

代码已 clean build 通过。

变量包括：

```text
g_cal_hold_request
g_cal_hold_active
g_cal_hold_charge_done
g_cal_hold_packet_active

g_cal_hold_packet_count
g_cal_hold_total_on_cycles

g_cal_hold_raw_min
g_cal_hold_raw_max
g_cal_hold_raw_average
g_cal_hold_raw_sum
g_cal_hold_raw_samples

g_cal_hold_fault
g_cal_hold_stop_reason
g_cal_hold_duration_ms
```

第一次500ms运行：

```text
cal_active        = 1
cal_charge_done   = 0
cal_packet_count  = 0
cal_raw_samples   = 0
```

但 DSS 最后 pause 后：

```text
vout_probe_stop_reason = TARGET_REACHED
```

也就是说：

```text
初始1400 raw充电最终完成
但 CAL_HOLD 状态机没有及时看到这个完成事件
所以没有进入 Hold 阶段
```

当前尚未确认是：

```text
A. DSS / Host调试访问干扰实时运行
```

还是：

```text
B. VOUT_LIMITED probe
   → CAL_HOLD
   状态交接本身有Bug
```

---

# 21. 当前唯一下一步

不修改功率参数。

仍保持：

```text
150kHz
1400 raw 初始充电
1380~1400 Hold
2-cycle packet
1450 hard limit
500ms hold
Comparator/TZ ON
```

只改变 DSS 执行方式。

推荐：

```text
CPU暂停
↓
写 g_cal_hold_request = 1
↓
DSS runAsynch()
↓
F28034自行连续运行

主机等待约700~800ms
期间：
不halt
不读target memory
不refresh Expressions
不修改变量

↓
最后 halt 一次
↓
一次性读取最终结果
```

---

# 22. 下一轮必须读取的数据

```text
g_cal_hold_active
g_cal_hold_charge_done

g_cal_hold_packet_count
g_cal_hold_total_on_cycles

g_cal_hold_raw_min
g_cal_hold_raw_max
g_cal_hold_raw_average
g_cal_hold_raw_samples

g_cal_hold_duration_ms

g_cal_hold_fault
g_cal_hold_stop_reason

g_vout_probe_stop_reason

TZFLG.OST
g_pwm_enabled
```

真正证明 CAL_HOLD 已跑起来的核心：

```text
charge_done = 1

packet_count > 0

duration_ms ≈ 500ms
```

若 runAsynch 后正常：

```text
优先判断为DSS/Host观察方式干扰
```

若仍然：

```text
charge_done = 0
packet_count = 0
```

则停止功率推进，直接排查：

```text
VOUT probe完成
↓
CAL_HOLD接管
```

的软件状态交接。

---

# 23. 后续路线

CALIBRATION_HOLD 通过后：

```text
500ms Hold
↓
2s Hold
↓
万用表稳定测 VOUT
↓
获取：
VOUT_meter
ADC raw_average
ADC raw_zero
↓
建立本板真实标定：

VOUT = (raw - raw_zero) × K
```

然后求：

```text
本板真正：
12.000V = ? ADC raw
```

之后才进入：

```text
受控软启动
↓
12V目标
↓
开环稳态
↓
PI / PFM闭环
↓
分级带载
↓
100W
```

---

# 24. 示波器恢复后必须补做

当前 ADC Capture 只能看 VOUT 包络，不能替代功率波形。

示波器恢复后必须检查：

```text
SU1 / SU2
Vds
Vgs
谐振电流
IPRI
ZVS
Vds过冲
振铃
变压器偏磁
```

没有完成这些之前：

```text
不批准长期连续功率运行
```

---

# 25. 项目中已经出现的高价值调试案例

这些内容应保留作为未来简历 / 面试项目经验。

## 案例1：ADC滤波变量语义错误

```text
IIR内部16×累加器
被误当 filtered_raw
```

通过变量语义和数值关系定位并修复。

---

## 案例2：D10整流桥极性错误

通过：

```text
原理图
实物通断
电阻
IPRI方向
```

定位 CT 整流桥正负端错误并返修。

---

## 案例3：Comparator保护掩盖了软件自动停机Bug

原先：

```text
Comparator提前Trip
```

使系统表面看起来“PWM会自动停”。

qualification过滤首沿毛刺后才暴露：

```text
Single-cycle stop ISR根本没有正确执行
```

最终定位：

```text
PieVectTable.EPWM1_INT仍指向rsvd_ISR
```

---

## 案例4：软件显示PWM关闭，但硬件OST并未锁存

继续追踪发现：

```text
ISR写TZFRC缺少EALLOW/EDIS
```

因此：

```text
软件认为关了
硬件实际上没有锁
```

最终改为使用：

```text
TZFLG.OST
```

作为真正硬件证据。

---

## 案例5：ePWM ISR延迟2.75us

通过时序记录发现：

```text
CTR=ZERO → ISR ≈2.75us
```

实现：

```text
PROBE_INTERRUPT_ISOLATION
```

屏蔽 Group1 非必要中断后：

```text
真实功率下：
IRQ latency ≈0.42us
```

证明根因是：

```text
CPU/PIE中断竞争
```

---

## 案例6：示波器暂时失效后，用 F28034 自己做“黑匣子”

开发：

```text
ePWM1 SOCA同步 ADC Capture
```

每个周期记录 VOUT。

首次得到完整1ms VOUT建立轨迹，并证明：

```text
全桥
→ LLC
→ 变压器
→ 副边
→ 输出电容
```

真实传能已经建立。

---

# 26. 当前工程状态一句话总结

> 目前 150kHz 已成为可信的 Bring-up 基准频率，真实功率下已完成从单周期到 VOUT=1450 raw 附近的受控能量注入，Comparator/TZ、ePWM ISR、OST硬件锁存、同步ADC和VOUT目标停机均已实板验证。当前不再提高输出电压，而是正在开发 CALIBRATION_HOLD_PROBE，通过低能量2-cycle Burst把输出维持数百毫秒，用于万用表完成本板 VOUT ADC 的第一次真实标定。当前唯一待解决问题是 CAL_HOLD 初始充电完成后状态机未进入 Hold，下一步用 DSS `runAsynch()` 排除 Host 调试访问干扰，再判断是否存在状态交接 Bug。

---

# 27. 新会话恢复指令

如果上下文丢失，把本文件交给新的 ChatGPT，并告诉它：

> 从这份开发日记继续。当前唯一任务是完成 CALIBRATION_HOLD_PROBE 的 500ms `runAsynch` 实测，不重新讨论前面的 Bring-up，也不要修改功率参数。先确认 DSS 不干扰目标实时运行，然后根据 `charge_done / packet_count / duration_ms` 判断是 Host 调试问题还是 CAL_HOLD 状态交接 Bug。


---

## 16. PROFILE_C_VOUT_TARGET_LADDER_V1（2026-08-17）

Profile C 保持完全不变（250kHz/DB110 平台 15 cycles → DB36、250→150kHz、DB36 Phase C ≤150 cycles、DAC300、qualification、Comparator/TZ）。

新增 VOUT 目标阶梯：

- `g_accel_vout_target_raw`：仅允许 1200 / 1400，其它值 REJECT 不启动真实功率（默认 1200）
- 硬限值固件固定映射：1200→1300、1400→1450，无 CCS 可写变量，防止误放大（formal OVP 未校准前这是 Bring-up 安全门）
- runtime VOUT 判断只用**本周期 fresh** PWM-sync 采样（SOCA/EOC freshness + `g_adc_pwm_sync_valid` 门），连续 miss ≥ 3 立即 OST
- 每个 fresh sample：hard limit 优先、target 次之，命中即在**同一周期** scheduled OST（不等 5ms 任务）
- 总窗口 485 cycles 不变；结束未达 target → `MAX_CYCLES_REACHED`，不自动延长
- 停止瞬间完整冻结快照（target/hard/stop/max raw、cycles、phase、TBPRD/CMPA/CMPB/DBRED/DBFED/DACVAL、run-id 链、TZFLG、fault、SOCA/EOC/miss 计数）
- Immediate Truth 保留（runtime_before_ost + OST+5/10/20/50/100us），5ms Slow POSTSTOP 仅留档不再作 runtime 准确性判据
- 删除旧 300-raw（Phase C 诊断目标）与 800-raw（辅助 hard stop）语义

第一枪：RUN_ID `0x250C1200`、target 1200 / hard 1300、Vin 24V、0.20A、CNT3/CNT4 连接、完整 Cold Shot。1400 目标已预留（下一枪无需重新编译），本轮禁止执行。

---

# 28. STAGE5 正式 SoftStart 实板 10V 验收（2026-08-17）

把实板已验证的 Profile C 轨迹正式化为 SoftStart 引擎（`app/soft_start.c`），不再依赖历史探针路径。

## 28.1 正式引擎设计

- 轨迹（默认 Profile，实板验证）：250kHz/TBPRD239/CMPA120/CMPB60/DB110 ×15cyc → DB110→36（5/步×10cyc）→ 周期 239→399（+10/步×10cyc）→ 150kHz/399/200/100/DB36
- ePWM 周期驱动：`SoftStart_FastUpdate()` 在 EPWM1_INT_ISR 内执行，非 5ms 任务
- 验收模式（`g_softstart_acceptance_mode`）：VOUT raw ≥ 1244（标定 10V）→ 计划 OST；硬顶 1491（12V）→ 立即 OST；生产模式（0）轨迹完成后进 RUN
- 校准门：`BOARD_VOUT_CAL_VALID != 1` 拒绝真实功率启动
- 无能量仿真：`g_softstart_no_energy`（400/900/1260 按阶段，FINAL 命中 1244）

## 28.2 无能量验收（RUN_ID 0x250C5000）

result=2（ACCEPT_TARGET）、fault=0、346 周期全轨迹、soca=346=eoc、miss=0、终点 TBPRD=399/CMPA=200/CMPB=100/DB36、计划 OST 锁存。

## 28.3 实板单发 10V 验收（RUN_ID 0x250C5001）—— PASS

```
result=2  fault=0  cycle_count=428  final_cycles=82
last_vout=1247 ≥ 1244（≈10.02V）  vout_max=1247=stop_raw（无超调）
TBPRD=399/CMPA=200/CMPB=100/DB36  soca=398=eoc  miss=30(consecutive=0)
TZ=4(OST)  pwm=0  ost=1  run_id 链一致
```

真实功率下 VOUT 从 0 爬升，FINAL 阶段第 82 周期命中 10V 即停，硬顶 1491 未触碰。

## 28.4 过程中修复的 4 个根因

| # | 症状 | 根因 | 修复 |
|---|---|---|---|
| 1 | fault=0x8000 | 陈旧 soft_start.obj：单独 -O2 编译行缺 `--obj_directory`，链接用了旧 obj | bat 补 `--obj_directory="%BUILD%"` |
| 2 | fault=0x08 拒启 | 250kHz/239 超生产包络 399-428，缺诊断许可 | 脚本前置 `g_diag_frequency_override=1`（与 shot_* 一致） |
| 3 | STALE_ADC 中止 | StartPwmFormal 缺 `ADC_SetPwmSyncTriggerMode()` | 补 ADC 同步（CAL_HOLD 同款） |
| 4 | fault=0x40 OVF 风暴 | 250kHz 下转换批次 EOC2 距周期边界 ~50 ticks 竞争；ISR 清 ADC 标志无 EALLOW → flag 清不掉 → ISR 风暴（15 万次/500ms） | ① ISR/CheckOverflow 寄存器写 EALLOW ② 斜坡全程禁用 PIEIER1.INTx1（FastUpdate 自持新鲜性）③ ISR OVF→fault 加 `g_softstart_ramp_active` 守卫 ④ request 防御性清 probe/CAL_HOLD 分支标志（.ebss 不被 loadProgram 清零） |

严格模式（no_energy_test_mode=0）7/7 连续 PASS 后实板一次通过。

## 28.5 关键经验

- **F2803x 的 ADC 寄存器写必须 EALLOW**（ADCINTFLGCLR/ADCINTOVFCLR/SOC 配置），否则静默失效——同类问题第二次出现（前一次是软件 force）
- **RAM 加载模型下 .ebss 不被 loadProgram 清零**，跨运行残留可劫持 ISR 分支——request 路径必须防御性清零
- 250kHz（239 ticks）下 3×SOC 顺序转换（30MHz ADC 时钟）在 ~TBCTR 195 完成，EOC2 与周期边界竞争是 OVF 的物理根源

---

# 29. STAGE5A PFM 方向测试（2026-08-17）

## 29.1 任务目标

用最少两枪实板测试确认：24V 输入、当前 LLC 工作区中 frequency↑→VOUT? / frequency↓→VOUT?。禁止 PI/闭环/Stage6。

## 29.2 新增极小诊断模式（正式轨迹零修改）

- `g_pfm_direction_test_mode`：0=OFF / 1=TEST_150K / 2=TEST_170K；**>2 拒绝真实功率启动**
- 命中 1244 后：不立即 OST → 进入固定频率窗口（新状态 `SOFTSTART_PFM_WINDOW`=10）
  - TEST_150K：TBPRD=399/CMPA=200/CMPB=100/DB36，**45 complete cycles ≈ 300µs**
  - TEST_170K：TBPRD=round(60e6/170e3)−1=**352**（编译期实际计算）/CMPA=176/CMPB=88/DB36，**51 cycles ≈ 300µs**
- 窗口内继续 PWM-sync ADC；VOUT≥1491 → 立即 OST（HARD_VOUT_ABORT）；窗口完成 → scheduled OST（PFM_WINDOW_DONE=8）；**绝不进 RUN 闭环**
- 记录：start/end/max raw、timer2 elapsed、window cycles、频率/配置、SOCA/EOC/miss、run_id 链、最终 PWM/OST

## 29.3 无能量验证（3 phase，全 PASS）—— PFM_DIRECTION_NO_ENERGY_PASS

| 项 | TEST_150K (0x250C5A15) | TEST_170K (0x250C5A17) | 硬顶 (0x250C5A18) |
|---|---|---|---|
| result | 8 (PFM_WINDOW_DONE) | 8 | 3 (HARD_CEILING) |
| 窗口 | 45/45 cyc | 51/51 cyc | — |
| 配置 | 399/200/100/36, 150000Hz | **352/176/88/36, 169971Hz** | — |
| 时长 | **311.3µs** | **341.0µs** | — |
| 结束 | OST 锁存, fault=0 | 同 | OST, fault=0 |

## 29.4 过程中的缺陷修复

1. **窗口重复进入**：窗口内 raw=1260≥1244 每周期重触发 SS_EnterPfmWindow（重置计数）→ 无限循环；修复：PFM_WINDOW 状态跳过通用硬顶/验收检查（窗口 case 自管）
2. **Timer2 是递减计数器**：elapsed = st − et（TIM 递减，60MHz）——311µs 实测吻合 45×6.67µs
3. **RAM 预算耗尽**（.text 溢出 0x2100>0x2000）：llc_globals.c 的 0 初始化器改 ZEROINIT（.cinit 腾空间）——build 恢复 OK

## 29.5 下一步

实板两枪（每枪 Cold Shot + 用户确认，禁止自动连打）：
- 第一枪 TEST_150K → `PFM_DIRECTION_150K_PASS`
- 第二枪 TEST_170K → `PFM_DIRECTION_170K_PASS`
- 比较斜率（delta_raw / 真实 elapsed）→ CASE 判定（NORMAL / REVERSED / INCONCLUSIVE <10%）→ 生成 `docs/STAGE5A_PFM_DIRECTION_REPORT.md` → `STAGE5A_PFM_DIRECTION_ACCEPTED`

---

# 30. 当前工程状态一句话总结 + 下一会话恢复指令

> STAGE5 正式 SoftStart 实板 10V 验收 PASS（`FORMAL_SOFTSTART_10V_REAL_POWER_PASS`，RUN_ID 0x250C5001，result=2，fault=0，1247 raw≈10.02V 命中即停）。STAGE5A PFM 方向测试模式已实现并通过无能量 3-phase 验证（150k=311.3µs/45cyc、170k=341.0µs/51cyc/TBPRD352、硬顶在线、结束必 OST）。当前停在 `PFM_DIRECTION_READY_FOR_150K_SHOT`，等待用户确认台架（Vin 24V/0.20A/CNT3-4 连接/Cold Shot）后执行第一枪 TEST_150K（RUN_ID 0x250C5A15），只打一枪，禁止自动第二枪。

如果上下文丢失，把本文件交给新的 ChatGPT，并告诉它：

> 从这份开发日记继续。当前唯一任务是 STAGE5A PFM 方向测试第一枪：用户确认后执行 `tools/run_pfm_direction_shot.js`（TEST_150K，RUN_ID 0x250C5A15），PASS 后停，等待用户再次确认再打第二枪 TEST_170K（`run_pfm_direction_shot_170k.js`，RUN_ID 0x250C5A17）。两枪都 PASS 后自动比较斜率生成 `docs/STAGE5A_PFM_DIRECTION_REPORT.md` 并判定方向。禁止 PI、禁止闭环、禁止 Stage6、禁止自动连射。

---

# 31. OFFBENCH 虚拟板特征化（2026-08-17，用户不在工位）

## 31.1 任务
OFFBENCH_LLC_VIRTUAL_BOARD_CHARACTERIZATION_V1 + REMOTE_BENCH_HYBRID（虚拟部分）。
全程禁止真实功率/上电/改接线。所有工作在代码、原理图、EDA、已有实测、PC 模型上完成。

## 31.2 参数搜集（来源：100wllc_eda BOM+原理图PDF、development、analysis 模型）

| 参数 | 值 | 来源 |
|---|---|---|
| Lr | 3.35/3.385/3.42µH（标称 3.385µH） | SCHEMATIC |
| Lm | 16.9/17.25/17.6µH（标称 17.25µH） | SCHEMATIC |
| Cr | 原理图 330n×2+470n×5=3.01µF；BOM 330n×5+470n×2=2.59µF | **冲突**（analysis 采 3.004µF） |
| 匝比 | PQ3230 Np 5T : Ns_half 4T（n=1.25） | SCHEMATIC |
| MOSFET | BSC050N04LS（40V 级 ×4） | BOM |
| 整流 | MBRD10100CT（100V 2×10A ×4） | BOM |
| 驱动 | SI8233BD ×2 | BOM |
| 输出电容 | 470µF×5 = 2350µF | BOM |
| CT/R32 | XRPAEE5.0 1:100 / 10Ω → 0.1V/A | SCHEMATIC |
| DAC300 | 理论 9.668A | CALCULATED |
| 死区 | 600ns（36 ticks） | FIRMWARE |

**关键冲突/发现**：
1. Cr 有效值 50k vs 150k 之争——实板 24V/150k→10V 支持 fr≈150kHz（Cr≈0.33µF 情景）
2. 24V/12V 需 tank 增益 M≈1.32（容性区，ZVS 风险）——实板 24V 只能到 ~10V 是物理边界
3. 36V/12V 最容易（M≈0.88，66-88k 标称 / 170-190k 情景B）
4. **Lr 最敏感**（M150 ±8%）、Lm 最不敏感
5. 100W 满载原边电流接近理论 OCP 9.67A

## 31.3 产出
- tools/llc_virtual_plant.py（FHA 模型 + 频率扫描 CSV/PNG + 敏感性）
- tools/protection_model.py（OCP 理论映射）
- tools/llc_empirical_plant.py（实板斜率局部模型框架）
- tools/llc_control_sil.py（虚拟 ADC + STEP_PFM + PI + 闭环矩阵）
- docs/：VIRTUAL_BOARD_PARAMETER_INVENTORY / PARAMETER_CONFLICT_AUDIT / LLC_PARAMETER_SENSITIVITY /
  SOFTSTART_PHYSICS_EXPLANATION / ZVS_THEORETICAL_MAP / SEMICONDUCTOR_STRESS_ESTIMATE /
  TRANSFORMER_GAIN_AUDIT / PFM_DIRECTION_THEORETICAL / F28034_REALTIME_BUDGET /
  RETURN_TO_BENCH_MINIMUM_TEST_PLAN / OFFBENCH_VIRTUAL_BOARD_CHARACTERIZATION_V1（总报告）

## 31.4 SIL 结果
- STEP_PFM：可达区 8 PASS（方向正确无正反馈跑飞）；55 物理限制；2 精度案例保留
- PI-PFM：36V/5W、25W 12V ±1% PASS；0 控制失败（其余物理/OCP 限制保留）
- 保护：ADC miss3 / OVP / freq clamp / 模拟OCP 全 PASS
- plant 默认情景 B（fr≈150k，实板一致）

## 31.5 编译保护
```c
LLC_VIRTUAL_PI_VALIDATED = 1
LLC_HARDWARE_PI_VALIDATED = 0   /* 真实板禁闭环 */
LLC_POWER_RUN_ALLOWED = 0
```
OUT SHA256 = `545466b81e55db6c00027fa9e957f8f2a7b4575965c197feeb1034535b3ef7da`

## 31.6 待办（需用户在场/回工位）
- PFM 方向 150/170k 实板两枪（REMOTE_BENCH：需确认电源限流≤0.20A + 现场同事观察）
- LCR 实测 Lr/Cr/Lm/匝比（裁决 fr）
- MOS Vds 尖峰（36V，40V 器件裕量风险）
- ZVS 波形、OCP 安培标定、带载温升、24/30/36V 验证

---

# 32. STAGE6 物理 plant 模型整定与 PI SIL（2026-08-17 后半段，纯离线）

> 本阶段以 `tools/llc_physical_plant_v2.py` 为唯一物理静态模型，对真实 F28034 控制逻辑做**纯离线**整定（PC/SIL）。
> 全程禁止真实功率/烧板/改硬件控制门/写最终固件 Kp/Ki/解锁 PWM 闭环。
> 固件门恒为：`LLC_CONTROL_DIRECTION=0`、`LLC_HARDWARE_PI_VALIDATED=0`、`NO_REAL_POWER_EXECUTED`。
> 仅改 `tools/` 与 `docs/`。

## 32.1 物理模型基线（保留的已确认结论，不回退）

| 参数 | 值 | 定级 |
|---|---|---|
| Cr | 3.004µF | HARDWARE_MEASURED |
| Lr | 3.385µH（标称） | HARDWARE_MEASURED/DERIVED |
| Lm | 17.25µH（标称） | HARDWARE_MEASURED |
| n = Np/Ns_half | 1.25 | SCHEMATIC |
| Cout | 2350µF；Vf≈0.7V | BOM/ASSUMED |
| fr = 1/(2π√(Lr·Cr)) | ≈49.9kHz | HARDWARE_CONSTRAINED_RESONANCE |
| VOUT 标定 | gain=0.008089325 V/raw，offset=−0.063715 V | `app/board_calibration.h` |

`MODEL_B_NONPHYSICAL_FIT_RETIRED=1`（放弃早期用实板 24V/150k→10V 反推的“非物理拟合情景 B”）。

## 32.2 全桥 FHA 直流换算约定修正（关键，此前 double-conversion 错误）

在 `Rac=8n²RL/π²`、`M=|Zm/(Zr+Zm)|`、`n=Np/Ns_half` 这套 FHA 下：
- **FULL BRIDGE**：`Vout = Vin·M/n − Vf`（k_dc = 1.0）
- **HALF BRIDGE**：`Vout = Vin·M/(2n) − Vf`
- 禁止再用 `(8/π²)·Vin·M/n`：8/π² 只出现在 Rac/AC-equivalent 推导，若已用 `Rac=8n²RL/π²` 再乘一次就是 **double conversion**。

谐振点单位校验（f=fr，Zr≈0，M≈1）：FULL BRIDGE 应得 `Vout_ideal ≈ Vin/n − Vf`，Vin=24/n=1.25 → **18.5V**（而非 8/π² 给出的 14.86V）→ `FULL_BRIDGE_RESONANCE_GAIN_SANITY_PASS`。

## 32.3 STAGE6 plant 一致性裁决链

| 提交 | 内容 | 裁决 |
|---|---|---|
| be40e4f | Stage6 PI V1 保守候选 SIL 特征化（`stage6_pi_tuning.py`，**V1_HISTORICAL**，含旧 Cr=0.33u + `/(2n)`，不参与 V2 裁决） | V1 退役 |
| b484999 | plant 模型 vs 硬件 reconcile | `MODEL_HARDWARE_CONSISTENCY_PASS` → 后因换算/校准漏洞标记 **SUPERSEDED_PENDING_V1_1_REVIEW** |
| a2e2e48 | 修正 FHA 全桥换算 + VOUT 标定来源 | **MODEL_HARDWARE_CONSISTENCY_PASS_V1_1**（+`BOARD_CALIBRATION_SOURCE_OF_TRUTH_PASS`） |
| 27d0399 | reachability 分类修正 + 连续 PFM 区域推导 | **REACHABILITY_CLASSIFIER_PASS / CONTROL_REGION_MAP_PASS / PI_V2_CASE_SET_VALIDATED** |
| d506890 | Stage6 PI V2 在已验证连续 PFM 区整定 | **STAGE6_PI_SIL_TUNING_V2_PASS** |
| 51a4b94 | 关闭 V2 SIL 指标/时序审计洞 | **STAGE6_PI_SIL_TUNING_V2_1_PASS** |

## 32.4 Reachability（V1_2）关键结论

- 判定修正：`reachable = (vmin−0.05 ≤ Vref ≤ vmax+0.05)`，四态分类（TARGET_REACHABLE / BELOW / ABOVE / NON_MONOTONIC），1kHz 步进 + 二分 f_target_12V（±100Hz，无 scipy）。
- **CONTINUOUS_PFM_REGION**（120–180k 内可连续 PFM 到 12V）：24V/50W、24V/75W、30V/100W、36V/100W。
- **LIGHT_LOAD_HIGH_FREQUENCY_OR_BURST_REGION**（全窗 >12V，需升频或未来 Burst）：24V/5W、25W、30V/5–75W、36V/5–75W；30V/5W、36V/5W、36V/25W 明确 `CONTINUOUS_PFM_120_180K_CANNOT_REGULATE_DOWN`。
- **LOW_VIN_HEAVY_LOAD_LOW_FREQUENCY_REGION**：24V/100W（`CANNOT_REGULATE_UP`，80–250k 模型探索交点 107kHz，`LOW_FREQUENCY_OPERATION_REQUIRES_ZVS_OCP_REVALIDATION`，不得直接上板）。
- 扩展探索窗 80–250k 仅 `MODEL_EXPLORATION_WINDOW`/`NOT_PRODUCTION_LIMIT`，未改固件频率范围（保持 120–180k）。
- PI V2 工况集：`PI_V2_CONTINUOUS_PFM_CASES`={24V/50W, 24V/75W, 30V/100W, 36V/100W}，其余进 `PI_V2_EXCLUDED_CASES`（BURST/above/below-window）——**物理不可调点不算 PI 失败**。

## 32.5 PI SIL V2 / V2.1 结果

- 唯一物理模型复用 `llc_physical_plant_v2.py`（FHA，Vout=Vin·M/n−Vf）；`CONTROL_DESIGN_SURROGATE`：`dV/dt=(Vss(f,Vin,load)−V)/tau`，tau 动态不确定性 ensemble {0.5,1.5,3,6,10}ms（`DYNAMIC_TIME_CONSTANT_NOT_HARDWARE_IDENTIFIED`）。
- 控制器**镜像真实 control.c**：error=Vref−Vout、SIGN=−1、bias=150kHz、Imax=±60000、clamp 120–180k、slew 100Hz/20µs、conditional hard-clamp AW、ADC stale(≥3) freeze。
- ePWM 量化镜像 driver/pwm.c：`TBPRD=round(60e6/f)−1`，150k→399、170k→352（actual≈169971Hz）→ `EPWM_FREQUENCY_QUANTIZATION_MODEL_PASS`。
- 解析种子：`Kp≈2π·fc·tau/|Kf|`，`Ki_step≈2π·fc/|Kf|·20µs`，fc∈{10,20,30,50,80} × seed×{0.5,1,2} 细扫。
- **候选（VIRTUAL_ONLY_PI_CANDIDATE，非 HARDWARE_TUNED）**：
  | 候选 | scaled_fc | Kp(Hz/V) | Ki_step(Hz/(V·20µs)) |
  |---|---|---|---|
  | A ULTRA_CONSERVATIVE | 20 | 6657.43 | 44.38 |
  | **B BALANCED（推荐）** | 20 | 6657.43 | 44.38 |
  | C FASTEST_SAFE | 160 | 53259.47 | 355.06 |
- V2.1 修正审计后全部 PASS：settling 改用 0.02V 带从 step 时刻起算（worst overshoot 0.109V、settle 25.8ms）、delay off-by-one 修正（20/40/60µs→step1/2/3）、boundary AW strict（clamp 处积分器冻结 + 反向可离开 clamp）、command-side rate-limit 审计、stress 检查 bounded/no-runaway/clamp-ping-pong、per-case 确定性 RNG。
- 完整重搜索（J）独立复现 BALANCED=Kp6657.43/Ki_step44.38 → `STAGE6_PI_SIL_TUNING_V2_1_PASS`。

## 32.6 产出文件（tools/ + docs/）

- `tools/llc_physical_plant_v2.py`（唯一物理模型 + 校准解析）、`tools/stage6_reachability_v1_2.py`（reachability/控制区域分类）、`tools/stage6_pi_tuning_v2.py`（V2.1 整定/审计 harness）、`tools/stage6_pi_tuning.py`（V1_HISTORICAL 保留）
- `docs/STAGE6_PHYSICAL_PLANT_RECONCILIATION.md`（→PASS_V1_2）、`STAGE6_TRANSFORMER_RECTIFIER_GAIN_DERIVATION.md`（修正推导）、`STAGE6_CONTROL_REGION_MAP.csv`、`STAGE6_CONTROL_REGION_SUMMARY.md`、`STAGE6_PI_V2_LOCAL_PLANT.csv`、`STAGE6_PI_V2_CANDIDATES.csv`、`STAGE6_PI_V2_TUNING_REPORT.md`、`STAGE6_PI_V2_ACQUISITION_REPORT.md`

## 32.7 阶段门

固件门恒锁：`LLC_CONTROL_DIRECTION=0`、`LLC_HARDWARE_PI_VALIDATED=0`、`NO_REAL_POWER_EXECUTED`。候选为 VIRTUAL_ONLY，仅供下一授权阶段的 firmware shadow 集成，**不得**直接解锁 PWM 闭环或真实功率。

---

# 33. 当前工程状态一句话总结 + 下一会话恢复指令

> Stage 5A PFM 方向已由真实功率证据接受（150k/170k same-binary 实测，150k>170k）。Stage 6 plant 模型与 PI 候选已**纯离线**完成并逐级裁决至 `MODEL_HARDWARE_CONSISTENCY_PASS_V1_2` + `STAGE6_PI_SIL_TUNING_V2_1_PASS`，推荐 BALANCED（Kp≈6657.43 Hz/V，Ki_step≈44.38 Hz/(V·20µs)）为 `VIRTUAL_ONLY_PI_CANDIDATE`。固件门保持锁死（`LLC_HARDWARE_PI_VALIDATED=0`、`LLC_CONTROL_DIRECTION=0`）。

如果上下文丢失，把本文件交给新的 ChatGPT，并告诉它：

> 从这份开发日记继续。当前阶段：Stage 6 物理 plant 模型已通过 `MODEL_HARDWARE_CONSISTENCY_PASS_V1_2`，reachability/控制区域已通过 V1_2，PI V2 候选已通过 `STAGE6_PI_SIL_TUNING_V2_1_PASS`（推荐 BALANCED：Kp≈6657.43、Ki_step≈44.38，标记 VIRTUAL_ONLY_PI_CANDIDATE）。所有工作仅改 tools/ 与 docs/，固件门锁死（LLC_HARDWARE_PI_VALIDATED=0、LLC_CONTROL_DIRECTION=0），禁止真实功率/禁止解锁 PWM 闭环/禁止写最终固件 Kp/Ki。下一步需用户明确授权才可进入任何 firmware/domain 集成阶段。

---

# 34. STAGE6 PI FIRMWARE SHADOW INTEGRATION V1（2026-08-17，固件 SHADOW 接入，纯离线）

> 把 V2_1 BALANCED 候选正式接入固件 **SHADOW** 控制路径，但真实 PWM 仍被硬门隔离。
> 允许改固件 / FLASH build / 离线 no-energy 测试；禁止真实功率/真实闭环/清除写门/连续12V/负载。

## 34.1 数值语义一致性（B/C/D）—— 先行门，通过才写 control.c
- `tools/stage6_pi_firmware_numeric_parity.py`：float32(IEEE754)+`(Uint32)out` 整数提交，镜像 control.c 全部中间量；重放 mandatory ensemble（24V/75W、30V/100W；gain 0.5/1/2；tau 1.5/3/6ms；delay 20/40µs；Vref±0.1；load±10%；noise±1/2/4raw；stale 3）。
- **C28X_FLOAT32_PI_PARITY_PASS=True**、**UINT32_FREQUENCY_COMMIT_PARITY_PASS=True**（int-commit freq_pp 278/348Hz、TBPRD span 1、无拍边）。
- D 对比 double SIL：max|Vout|=0.00032V、max|freq|=2.76Hz、max|I|=0.83Hz，无新多kHz limit cycle，PASS/FAIL 结论不变。

## 34.2 固件改动（仅 SHADOW，无真实 PWM）
- 新增 `app/control_profile.h`：唯一正式 shadow 候选 BALANCED（Kp=6657.43331f、Ki_step=44.3828888f、ID 0x060201、VIRTUAL_ONLY=1、HARDWARE_VALIDATED=0）+ 内部 `#if` 自检；来源 V2_1 commit 51a4b94；不加入 FAST profile 到运行代码。
- `app/control.c`：删 CTRL_KP=0.0005/CTRL_KI=0.0001 占位，改用 profile 宏；跨门 `#if CTRL_PI_PROFILE_HARDWARE_VALIDATED && !LLC_HARDWARE_PI_VALIDATED #error`；`CTRL_Init()` 加教学变量 g_control_pi_profile_id/kp/ki_step/virtual_only。算法其它逐字不变。
- `app/llc_globals.h/.c`：新增 4 个教学 globals。
- `tools/check_control_profile_sync.py`：**PI_PROFILE_SOURCE_SYNC_PASS=True**。
- `tools/stage6_pi_shadow_sanity.py`：**STAGE6_BALANCED_PROFILE_OFFLINE_SELFTEST_PASS=True**（7 case）+ **BALANCED_PROFILE_FIRST_STEP_SANITY_PASS=True**（150000→149900/150100）+ ADC stale freeze/recovery PASS。

## 34.3 Stage6_FLASH build（M/O/H）
- CGT 25.11.1.LTS `--abi=coffabi` clean build：**BUILD OK，0 unresolved，0 overflow**（`tools/build_flash.bat`）。
- OUT SHA256 `16DF761E3F139A76693B66F0B2C68BACEFAA199C5BC345FE8D804E0DD58272C6`，MAP SHA256 `E5FB0E6FBEF26414C669C0527C21260F91D27343C9D8B2C077025E1474BC54F0`；FLASH used 9980/65408（15.3%，裕量 55428）；数据 RAM used 828/6144（13.5%）。
- 反汇编 `control.obj`：Compute 含 BALANCED 常量（0x45d00b77=6657.43331、0x42318814=44.3828888）；`_CTRL_ApplyFrequencyCommand` 仅 7 words 纯 shadow commit，**无 LLC_SetFrequencyHz 调用** → **PI_PROFILE_PRESENT_PWM_WRITE_GATE_LOCKED_PASS=True**。
- 候选存在**不会**自动解锁 hardware；候选与 `LLC_HARDWARE_PI_VALIDATED=0` 完全独立。

## 34.4 文档
- `docs/STAGE6_PI_FIRMWARE_SHADOW_INTEGRATION.md`（S 主文档：binary 有参数 ≠ 有硬件控制权，架构 ADC/synthetic→BALANCED→shadow→X real ePWM）、`docs/STAGE6_PI_EXECUTION_BUDGET_PLAN.md`（N：TINT0_ISR(protection.c:185)→CTRL_FastTask→Compute→Apply 预算测量设计，不编造 µs）、`docs/STAGE6_PI_FIRMWARE_NUMERIC_PARITY.md`、`docs/STAGE6_PI_SHADOW_SANITY.md`。

## 34.5 阶段门
`LLC_CONTROL_DIRECTION=0`、`LLC_HARDWARE_PI_VALIDATED=0`、`NO_REAL_POWER_EXECUTED`；`READY_FOR_ON_TARGET_SHADOW_NOENERGY_TEST`（下一阶段单独授权：RAM/no-energy 跑 8-case on-target + 20µs 预算实测）。test_static.py 3 项 FAIL 为既有/环境性（Debug/.out 未建、launch 命名、broad "CAN" 扫描），非本任务引入。
